pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r .codex/skills/neurovi-workspace-dashboard/output/* public/
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH'
